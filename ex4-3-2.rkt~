;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex4-3-2) (read-case-sensitive #t) (teachpacks ((lib "convert.rkt" "teachpack" "htdp"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "convert.rkt" "teachpack" "htdp")) #f)))
;; the second cond-expression is illegal
;; it is illegal because its second line contains one expression instead of two
;; there is only the condition on that line - no answer

;; re: the other question, the cond-epxression is illegal because [* 10 n] is one expression, not two
;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex8-7-1) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
(define PRICE 5)
; (define SALES-TAX (* 0.08 PRICE))
; (define SALES-TAX (* 0.08 5))
(define SALES-TAX 0.40)
; (define TOTAL (+ PRICE SALES-TAX))
; (define TOTAL (+ 5 SALES-TAX))
; (define TOTAL (+ 5 0.40))
(define TOTAL 5.40)